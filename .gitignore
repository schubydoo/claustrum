scratch/
/dist/
/claustrum
/claustrum.exe

# mkdocs build output + local docs toolchain venv
/site/
/.venv/

# Host-local agent guardrails (host specifics + context-mode routing; never committed)
CLAUDE.local.md

# Go build & test artifacts
*.test
*.out
coverage.*

# OS / editor cruft
.DS_Store
.nfs*
