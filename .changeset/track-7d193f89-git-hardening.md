---
default: minor
---

Track reference `claude-ssh` build `7d193f89`: a repository whose git config cannot be enumerated is refused rather than run unhardened, `git.worktree_create` failure output is capped at 512 bytes, and git operations honour the user's global excludes file.
