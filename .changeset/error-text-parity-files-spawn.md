---
default: patch
---

Report the reference daemon's error text when `files.list` is given a non-directory, when `files.extract_tar` cannot open the archive, and when `process.spawn` is given an unusable `cwd`.
