---
default: minor
---

Git reads and worktree creation run under the reference's git-invocation hardening profiles, and `server.capabilities` advertises the new `git.status.baseRepo` and `git.worktree.external_root` features.
