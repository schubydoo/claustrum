---
default: patch
---

claustrum now pins reference build `4534d86`: `git.status` of a linked worktree runs through an isolated temp gitdir matching the reference (byte-identical on Linux and macOS), the one Windows divergence D16 is documented (the reference's own git.status of a linked worktree errors on Windows while claustrum returns the correct status), and the reference-build ledger and upstream-tracking are updated.
