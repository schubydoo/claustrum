---
default: minor
---

Track reference `claude-ssh` build `7d193f89`: a new worktree is seeded only with untracked files named by both `.worktreeinclude` and git's ignore rules, and `.claude/` is no longer copied unconditionally.
